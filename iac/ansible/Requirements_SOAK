Requirements
    - Infra
        - Go Lang
            
        - I need Downlaod the file.
             - https://docs.ansible.com/ansible/2.8/modules/unarchive_module.html#examples 
             

        - Docker(package)
                remove old version (docker docker-engine docker.io containerd runc)
                add packages
                  ubuntu:
                    apt-transport-https
                    ca-certificates
                    curl
                    gnupg
                    lsb-release
                  Add gpg key
                    curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
                    docker-archive-keyring.gpg
                Setup repo
                Update
                Install docker
                    can specify version
                        docker-ce=<VERSION_STRING>
                        docker-ca


                  CentOS:
                    remove packages
                       docker-client
                       docker-client \
                       docker-client-latest \
                       docker-common \
                       docker-latest \
                       docker-latest-logrotate \
                       docker-logrotate \
                       docker-engine
                    Install package (fedora dnf -y install dnf-plugins-core)
                        yum-utils
                    add repo (Differ from Fedora)
                    enable repo (Optional)
                    install docker
                    enable docker (service docker)
                    add user to docker



             - https://docs.ansible.com/ansible/2.8/modules/list_of_packaging_modules.html
             - https://docs.ansible.com/ansible/2.8/modules/apt_module.html#examples


        - Docker-compose
            download binary from https://github.com/docker/compose/release/download/
                ex: Bolt
                curl -L "https://github.com/docker/compose/release/download/$PT_release/docker
            specify version
            specify version


             - https://docs.ansible.com/ansible/2.8/modules/docker_compose_module.html#examples

    tools
        - Flamethrower
        - Dhammer
        - Howitzer

Analisar os requisitos de infra e relacionar com os modulos ansible para poder criar o ambiente.
